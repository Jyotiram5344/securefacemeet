# Optional: override API base URL for axios.
# Leave unset in Vite dev to use the dev-server proxy (same origin → /api → backend :8000).
# For direct calls (e.g. testing from file:// or custom port), set explicitly:
# VITE_API_BASE_URL=http://127.0.0.1:8000
